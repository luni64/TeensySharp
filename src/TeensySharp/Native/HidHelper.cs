using System;
using System.Collections.Generic;
using System.IO;
using System.Text;
//using static libTeensySharp.Native.NativeMethodsOld;
using BetterWin32Errors;
using HidLibrary;

namespace lunOptics.libTeensySharp
{
    //internal static class HidHelper
    //{
    //    public static void test()
    //    {

    //        var d = HidDevices.Enumerate();

    //        string Path = "c:\\MK64F12.svd";
    //        var handleValue = CreateFile(Path, EFileAccess.FILE_GENERIC_READ, EFileShare.None,IntPtr.Zero, ECreationDisposition.OpenExisting, 0, IntPtr.Zero);
    //        if(handleValue.IsInvalid) throw new Win32Exception("getConnectedDevcieIDs");

    //        handleValue.Dispose();
    //    }
    //}
}
